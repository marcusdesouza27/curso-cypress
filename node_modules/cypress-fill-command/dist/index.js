"use strict";
Cypress.Commands.add('fill', {
    prevSubject: 'element'
}, function (subject, value) {
    var _a, _b;
    var element = subject[0];
    var nativeInputValueSetter = (_a = Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype, 'value')) === null || _a === void 0 ? void 0 : _a.set;
    var nativeTextAreaValueSetter = (_b = Object.getOwnPropertyDescriptor(window.HTMLTextAreaElement.prototype, 'value')) === null || _b === void 0 ? void 0 : _b.set;
    if (element.tagName.toLowerCase() === 'input') {
        nativeInputValueSetter === null || nativeInputValueSetter === void 0 ? void 0 : nativeInputValueSetter.call(element, value);
    }
    else {
        nativeTextAreaValueSetter === null || nativeTextAreaValueSetter === void 0 ? void 0 : nativeTextAreaValueSetter.call(element, value);
    }
    var inputEvent = new Event('input', { bubbles: true });
    element.dispatchEvent(inputEvent);
    Cypress.log({
        name: 'fill',
        message: value,
        $el: subject,
        consoleProps: function () {
            return {
                value: value
            };
        }
    });
});
